# Dev Container

This Codespace is based on a prebuilt Python image and uses `environment.yml` for dependencies.  
No Dockerfile is required.

## Features
- Python 3.11 + Conda
- JupyterLab, NumPy, Pandas, Matplotlib, Scikit-learn
- VSCode extensions pre-installed
